services:

  nessie:
    hostname: nessie-catalog
    image: 'bitnami/nessie:latest'
    ports:
      - 191210:19120
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    networks:
      - default

